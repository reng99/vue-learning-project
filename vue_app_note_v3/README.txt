STEP0:
	you can open the file named "index.html" to see the effect

STEP1:
	npm install

STEP2:
	grunt runLess

STEP3:
	after do the step2,you can find that the file named "css" had created.this file has a file of css which had changed from file of less(style.less).
	you can do the step0 to see the effect.

attension:
	this project use the skill with grunt.the grunt can change the css's style into less's style.Of course you should install some dependences.
	In this project, I install the grunt and grunt-config-less.